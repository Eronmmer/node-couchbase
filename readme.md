# Issue tracker with Node.js and Couchbase

- Download Couchbase server and create a bucket locally, using Docker or Couchbase cloud.

- Set your env variables. See `.example.env`.

- Install dependencies

- Enjoy.